# pre-existing agent-ops scaffolding (not part of this project)
cursor-rules/
AGENTS.md
.windsurfrules
.agent/
# ignore local agent-ops scaffolding under .github/, but track CI workflows
.github/*
!.github/workflows/
.cursor
.envrc

# local dev / editor
.claude/
.DS_Store

# build artifacts
site/dist/
node_modules/

# slop-mop working directory (machine-local state), except the committed
# dependency manifest the CI action installs gate tools from
.slopmop/*
!.slopmop/required-deps.json

# next.js app
/.next/
/out/
next-env.d.ts
.vercel
*.tsbuildinfo
.env*.local

# e2e test artifacts
tests/e2e/.features-gen/
tests/e2e/.playwright-report/
tests/e2e/report/output/
tests/e2e/test-results/

# Sentry Config File
.env.sentry-build-plugin

# demo recording artifacts (the served clips live in public/gallery/)
/tests/demos/raw/
/tests/demos/.auth.json
